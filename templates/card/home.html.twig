{% extends "base.html.twig" %}

{% block title %}Dice{% endblock %}

{% block content %}
<h1>{{ title }}</h1>

<p>Use the navbar below to navigate through the pages related to Card Deck:</p>

<div class="navbar">
    <nav>
        <ul>
            <a href="{{ url("card-deck") }}">Card deck</a>
            <a href="{{ url("card-deck-shuffle") }}">Card deck shuffle</a>
            <a href="{{ url("card-deck-draw") }}">Draw card from deck</a>


        </ul>
    </nav>
</div>


{% endblock %}
