<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset("favicon.ico") }}">
        <link rel="stylesheet" href="{{ asset("css/styleSustain.css") }}"/>

        {% block stylesheets %}
        {# 'app' must match the first argument to addEntry() in webpack.config.js #}
        {{ encore_entry_link_tags('app') }}

        <!-- Renders a link tag (if your module requires any CSS)
             <link rel="stylesheet" href="/build/app.css"> -->
        {% endblock %}

        {% block javascripts %}
        {{ encore_entry_script_tags('app') }}

        <!-- Renders app.js & a webpack runtime.js file
            <script src="/build/runtime.js" defer></script>
            <script src="/build/app.js" defer></script>
            See note below about the "defer" attribute -->
        {% endblock %}

    </head>
    <body class="sustainBody">
        <div class="headerelement">
            <div class="siteheader">
                <img class="header_img center" src="{{ asset("img/sustain.png") }}" alt="balconies with green">
            </div>

            <div class="navbar">
                <nav>
                    <ul>
                        <a href="{{ url("home") }}">Tillbaka till huvudsidan</a>
                        <a href="{{ url("about") }}">About</a>

                    </ul>
                </nav>
            </div>
        </div>

        {% block content %}
        Default content
        {% endblock %}

        <div class="site-footer">
            Websidan är skapad av Martin Westman, som läser webbprogrammering vid BTH
            <p> <a href="https://dbwebb.se/kurser/mvc-v2">Länk till kursens websida</a></p>
        </div>

    </body>
</html>
